"""Setup for pip-installable package.

Metadata lives in ``pyproject.toml`` — this file exists only so the repo
matches the ALLFED template, and so ``pip install -e .`` works on older
tooling.
"""

from setuptools import setup

setup()
